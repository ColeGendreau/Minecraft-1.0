# One-time setup for new forks
# Run this FIRST after adding your Azure credentials and GitHub PAT
# It will automatically configure everything else you need

name: "0. Initial Setup (Run First!)"

on:
  workflow_dispatch:
    inputs:
      confirm_setup:
        description: 'Type "setup" to confirm you want to run initial setup'
        required: true
        type: string

permissions:
  id-token: write
  contents: read

jobs:
  validate:
    name: "Validate Inputs"
    runs-on: ubuntu-latest
    steps:
      - name: Check confirmation
        if: github.event.inputs.confirm_setup != 'setup'
        run: |
          echo "❌ Please type 'setup' in the confirmation field to proceed"
          exit 1

  setup:
    name: "Configure Everything"
    runs-on: ubuntu-latest
    needs: validate
    
    env:
      # Generate unique storage account name from repo (max 24 chars, lowercase alphanumeric only)
      STORAGE_RG_NAME: mc-demo-tfstate-rg
      STORAGE_CONTAINER_NAME: tfstate
    
    steps:
      - uses: actions/checkout@v4

      - name: "Step 1/6: Azure Login (with client secret)"
        uses: azure/login@v1
        with:
          creds: '{"clientId":"${{ secrets.AZURE_CLIENT_ID }}","clientSecret":"${{ secrets.AZURE_CLIENT_SECRET }}","subscriptionId":"${{ secrets.AZURE_SUBSCRIPTION_ID }}","tenantId":"${{ secrets.AZURE_TENANT_ID }}"}'

      - name: "Step 2/6: Create Federated Credential for OIDC"
        env:
          AZURE_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
          GITHUB_REPO_FULL: ${{ github.repository }}
        run: |
          echo "🔐 Setting up OIDC authentication..."
          
          # Get the app's object ID (different from client ID)
          APP_OBJECT_ID=$(az ad app show --id "$AZURE_CLIENT_ID" --query id -o tsv)
          
          if [ -z "$APP_OBJECT_ID" ]; then
            echo "❌ Could not find app registration with client ID: $AZURE_CLIENT_ID"
            exit 1
          fi
          
          echo "Found app object ID: $APP_OBJECT_ID"
          
          # Check if federated credential already exists
          EXISTING=$(az ad app federated-credential list --id "$APP_OBJECT_ID" --query "[?name=='github-actions-main'].name" -o tsv 2>/dev/null || echo "")
          
          if [ -n "$EXISTING" ]; then
            echo "✅ Federated credential already exists - skipping"
          else
            echo "Creating federated credential for repo: $GITHUB_REPO_FULL"
            
            # Create federated credential for main branch
            az ad app federated-credential create \
              --id "$APP_OBJECT_ID" \
              --parameters "{
                \"name\": \"github-actions-main\",
                \"issuer\": \"https://token.actions.githubusercontent.com\",
                \"subject\": \"repo:${GITHUB_REPO_FULL}:ref:refs/heads/main\",
                \"audiences\": [\"api://AzureADTokenExchange\"]
              }"
            
            echo "✅ Created federated credential for main branch"
          fi

      - name: "Step 3/6: Bootstrap Terraform State Storage"
        id: bootstrap
        env:
          STORAGE_ACCOUNT_NAME: mcdemodevtfstate
        run: |
          echo "📦 Setting up Terraform state storage..."
          
          # Check if resource group already exists
          if az group show --name "$STORAGE_RG_NAME" &>/dev/null; then
            echo "Resource group already exists"
          else
            echo "Creating resource group..."
            az group create --name "$STORAGE_RG_NAME" --location westus3
          fi
          
          # Check if storage account already exists
          if az storage account show --name "$STORAGE_ACCOUNT_NAME" --resource-group "$STORAGE_RG_NAME" &>/dev/null; then
            echo "Storage account already exists"
          else
            echo "Creating storage account: $STORAGE_ACCOUNT_NAME"
            az storage account create \
              --name "$STORAGE_ACCOUNT_NAME" \
              --resource-group "$STORAGE_RG_NAME" \
              --location westus3 \
              --sku Standard_LRS \
              --min-tls-version TLS1_2 \
              --allow-blob-public-access false
          fi
          
          # Get storage account key
          ACCOUNT_KEY=$(az storage account keys list \
            --account-name "$STORAGE_ACCOUNT_NAME" \
            --resource-group "$STORAGE_RG_NAME" \
            --query '[0].value' -o tsv)
          
          # Check if container exists
          if az storage container show --name "$STORAGE_CONTAINER_NAME" --account-name "$STORAGE_ACCOUNT_NAME" --account-key "$ACCOUNT_KEY" &>/dev/null; then
            echo "Container already exists"
          else
            echo "Creating container..."
            az storage container create \
              --name "$STORAGE_CONTAINER_NAME" \
              --account-name "$STORAGE_ACCOUNT_NAME" \
              --account-key "$ACCOUNT_KEY"
          fi
          
          echo "tf_state_key=$ACCOUNT_KEY" >> $GITHUB_OUTPUT
          echo "✅ Terraform state storage ready"

      - name: "Step 4/6: Generate Coordinator API Key"
        id: apikey
        run: |
          echo "🔑 Generating secure API key..."
          API_KEY=$(openssl rand -hex 32)
          echo "coordinator_api_key=$API_KEY" >> $GITHUB_OUTPUT
          echo "✅ API key generated"

      - name: "Step 5/6: Save Secrets to Repository"
        env:
          GH_TOKEN: ${{ secrets.GH_PAT }}
        run: |
          echo "💾 Saving auto-generated secrets..."
          
          # Set TF_STATE_ACCESS_KEY
          gh secret set TF_STATE_ACCESS_KEY --body "${{ steps.bootstrap.outputs.tf_state_key }}"
          echo "✅ TF_STATE_ACCESS_KEY saved"
          
          # Set COORDINATOR_API_KEY
          gh secret set COORDINATOR_API_KEY --body "${{ steps.apikey.outputs.coordinator_api_key }}"
          echo "✅ COORDINATOR_API_KEY saved"
          
          echo ""
          echo "🎉 Secrets configured automatically!"

      - name: "Step 6/6: Cleanup Temporary Secret"
        env:
          GH_TOKEN: ${{ secrets.GH_PAT }}
        run: |
          echo "🧹 Cleaning up temporary client secret..."
          
          # Delete the client secret from GitHub (no longer needed - OIDC is configured)
          gh secret delete AZURE_CLIENT_SECRET || true
          
          echo "✅ AZURE_CLIENT_SECRET removed from GitHub"
          echo ""
          echo "🔒 Future workflows will use OIDC (no secrets needed!)"

      - name: "🎉 Setup Complete!"
        run: |
          echo ""
          echo "=============================================="
          echo "   ✅ SETUP COMPLETE!"
          echo "=============================================="
          echo ""
          echo "Your fork is now configured. Next steps:"
          echo ""
          echo "1. Run '1. Control Plane (Dashboard)' workflow"
          echo "   → This deploys the dashboard and coordinator API"
          echo ""
          echo "2. Run '2. Minecraft Server' workflow"
          echo "   → This deploys the Minecraft infrastructure"
          echo ""
          echo "3. Visit your dashboard URL (shown after step 1)"
          echo ""
          echo "=============================================="
          echo ""
          echo "Repository: ${{ github.repository }}"
          echo "Branch: main"
          echo "Auth: OIDC (secure, no secrets stored)"
          echo ""
