# Dashboard Environment Configuration
# ================================
# This project runs entirely in Azure. These environment variables are 
# set automatically by GitHub Actions during deployment.

# Coordinator API URL (set automatically during CI/CD deployment)
# Example: https://mc-demo-dev-coordinator.azurecontainerapps.io
NEXT_PUBLIC_API_URL=

# Note: Local development is NOT supported. The dashboard and coordinator
# run as Azure Container Apps and require Azure infrastructure to function.

